{
    body: {
        {{img-view}}: {
            src: "{{data}}/download/{{img-name}}"
        }

        {{text-view}}: {
            centerInParent: true
            text: "{{attr}}"
            background: "#ffffff"
            textColor: "#000000"
            width: auto
            height: auto
        }
    }
}